[project]
name = "kurier"
version = "0.1.0"
description = "Django page for listing packages your waiting for"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.14.13",
]
